const FrontConnFactory = require("../factory/front-conn-factory");

class ManagerConnFactory extends FrontConnFactory {

    make(socket) {
        // TODO
        super.make(socket);
    }

}

module.exports = ManagerConnFactory;
